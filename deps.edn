{:paths ["src" "resources"]
 :deps {org.clojure/clojure {:mvn/version "1.12.0"}
        selmer/selmer {:mvn/version "1.12.62"}
        org.babashka/cli {:mvn/version "0.8.65"}
        cheshire/cheshire {:mvn/version "5.13.0"}
        ;; Pinned exactly: a parser behaviour change would move generated bytes
        ;; downstream. Pure Clojure, so it loads under Babashka as well as the
        ;; JVM — the non-Clojure yamlstar bindings need a native library and
        ;; would not.
        org.yamlstar/yamlstar {:mvn/version "0.1.17"}}
 :aliases
 {:test {:extra-paths ["test" "test-resources"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd"}}
         :main-opts ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}
  :build {:deps {io.github.clojure/tools.build {:mvn/version "0.10.5"}
                 slipset/deps-deploy {:mvn/version "0.2.2"}}
          :ns-default build}}}
